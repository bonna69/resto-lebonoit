{# templates/admin/dashboard.html.twig #}
{% extends '@EasyAdmin/page/content.html.twig' %}

{% block content %}
<div class="container mt-5">
    <h1 class="text-center mb-4">Admin Dashboard</h1>
    <div class="row">
        <div class="col-md-4">
            <div class="card text-center">
                <div class="card-body">
                    <h5 class="card-title">Manage Reservations</h5>
                    <p class="card-text">Create, update, and delete reservations.</p>
                    <a href="{{ path('admin', { entity: 'Reservation' }) }}" class="btn btn-primary">Go to Reservations</a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
